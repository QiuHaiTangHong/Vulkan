name: C/C++ CI

on:
  push:
    branches: [ "master" ]
  pull_request:
    branches: [ "master" ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v4
      with:
        submodules: recursive

    - name: 安装系统级依赖
      run: |
        sudo apt-get update
        sudo apt-get install -y \
          ninja-build \
          libxkbcommon-dev libwayland-dev libx11-dev \
          libxcursor-dev libxinerama-dev libxrandr-dev \
          libxi-dev libgl1-mesa-dev libvulkan-dev \
          libglfw3-dev libglm-dev

    - name: 安装 LLVM 22 和 libc++
      run: |
        wget https://apt.llvm.org/llvm.sh
        chmod +x llvm.sh
        sudo ./llvm.sh 22
        sudo apt-get install -y clang-22 libc++-22-dev libc++abi-22-dev

    - name: 运行依赖同步脚本
      run: |
        if [ -f "sync_deps.py" ]; then
          python3 sync_deps.py
        fi

    - name: 配置 CMake
      run: |
        cmake -B build -G Ninja \
          -DCMAKE_C_COMPILER=clang-22 \
          -DCMAKE_CXX_COMPILER=clang++-22 \
          -DCMAKE_BUILD_TYPE=Release \
          -DCMAKE_CXX_FLAGS="-stdlib=libc++" \
          -DCMAKE_EXE_LINKER_FLAGS="-stdlib=libc++"

    - name: 构建
      run: cmake --build build
